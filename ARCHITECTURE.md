# Bingo Fabrics - Project Architecture (ארכיטקטורת המערכת)

קובץ זה מתאר את המבנה המלא של האפליקציה, במטרה להפוך את תהליך הפיתוח, התחזוקה ואיתור התקלות לפשוט וברור יותר. המערכת מחולקת לשני חלקים עיקריים: השרת (Backend) שמבוסס על FastAPI, וצד הלקוח (Frontend) שמבוסס על React + Vite.

---

## 1. שרת (Backend) - Domain-Driven N-Tier Architecture
השרת בנוי בארכיטקטורת "שכבות מונחות-דומיין" (Domain-Driven N-Tier). המשמעות היא שכל פיצ'ר או ישות מרכזית מופרד לתיקייה משלו, ובה יש חלוקה ברורה של תחומי אחריות.

**מיקום:** `backend/app/`

### מבנה התיקיות המרכזיות בשרת:
- **`core/`**: הגדרות הליבה של השרת - קונפיגורציות סביבה (`config.py`), טיפול בשגיאות מערכת ופונקציות אבטחה בסיסיות.
- **`db/`**: חיבור והגדרות מסד הנתונים (SQLAlchemy) וכן סקריפטים לאתחול מידע ראשוני (`seed.py`).
- **`utils/`**: פונקציות שירות כלליות כמו שירותי אימייל (שליחת קבלות ועדכונים) וניהול מטמון (Cache).

#### תיקיית `domains/` - לב האפליקציה:
זוהי התיקייה החשובה ביותר בשרת. היא מכילה את הלוגיקה העסקית, מחולקת לפי נושאים (דומיינים):
1. **`admin/`**: ניהול פאנל המנהל (דו"חות, ניהול מלאי, עריכת הגדרות).
2. **`orders/`**: ניהול תהליך ההזמנה, בניית העגלה, עדכון הזמנות וחוקי העונות העסקיים.
3. **`products/`**: ניהול הקטלוג, מודלים של בדים, וצבעים (SKUs).
4. **`users/`**: אימות (Authentication), הרשמות (כולל לוגיקה חברתית כמו גוגל), והרשאות מנהל.

**בתוך כל אחת מתיקיות ה-Domain האלו תמצא את המבנה הקבוע הבא (N-Tier):**
- **`models.py`**: הגדרת הטבלאות במסד הנתונים (SQLAlchemy). אלו ה"אובייקטים" עצמם.
- **`schemas.py`**: הגדרות של Pydantic לטובת וידוא מידע (Validation). כל מה שנכנס ל-API או יוצא ממנו נבדק מול הסכמות האלו.
- **`services.py`**: כאן יושבת **הלוגיקה העסקית**. חישובים, שאילתות מורכבות למסד הנתונים והפעלת פונקציות. פונקציות אלו לעולם לא מדברות ישירות עם "האינטרנט" אלא מופעלות על ידי ה-Routes.
- **`routes.py`**: כאן מוגדרים ה-Endpoints עצמם (לדוגמה `GET /products/catalog`). ה-Route אחראי רק לקבל את הבקשה, לקרוא ל-`services.py` שמבצע את העבודה, ולהחזיר את התשובה ללקוח (Frontend).

---

## 2. צד לקוח (Frontend) - Feature-Based React
צד הלקוח בנוי בצורה שמאגדת את הקוד סביב **תכונות (Features)** ספציפיות במקום לפי סוג קובץ. ארגון זה מאפשר לעבוד על חלק באפליקציה (כמו עגלת הקניות) כשהכל מרוכז באותו מקום.

**מיקום:** `frontend/src/`

### מבנה התיקיות המרכזיות בלקוח:
- **`api/`**: הגדרת החיבור לשרת (קובץ ה-Axios Client `client.js`) וטיפול ראשוני בניתוב קריאות ל-Backend.
- **`layouts/`**: רכיבי UI שעוטפים עמודים שלמים ומופיעים לאורך כל האתר (לדוגמה ה-`Navbar` המרכזי של האתר, תפריט הצד - אם יהיה).
- **`components/ui/`**: רכיבי ממשק משתמש **גלובליים וטיפשים**. אלו רכיבים שאפשר להשתמש בהם בכל מקום (כפתורים מיוחדים, קרוסלת תמונות `ImageCarousel`, מצגות מודאליות `ImageLightbox`, סליידרים כגון `FabricLengthSlider`). רכיבים אלו לא מכילים לוגיקה עסקית ומקבלים הכל מבחוץ דרך ה-`props`.
- **`hooks/`**: רכיבים שיתופיים של React שמנהלים תהליכים או שואבים מידע ויכולים להיות בשימוש בכמה מקומות (לדוגמה, חיבור לכלי חיצוני כלשהו). אם ה-Hook ייעודי רק לפיצ'ר מסוים, הוא ישב בתוך התיקייה של הפיצ'ר.

#### תיקיית `features/` - לב התצוגה והלוגיקה:
כל פיצ'ר אפליקטיבי יושב בתיקייה משלו תחת `features`. 
1. **`about/`**: עמודי מידע (אודות, תקנון).
2. **`auth/`**: התחברות והרשמה (מכיל גם את ה-`AuthContext.jsx` שמנהל את הסטייט של המשתמש ברחבי האפליקציה).
3. **`cart/`**: ניהול עגלת הקניות המורחבת.
4. **`catalog/`**: הצגת מוצרים, בניית הקטלוג הראשי ותצוגת העמוד המורחב של המוצר (`ProductDetailView`).
5. **`admin/`**: ניהול מערכת האדמין מנקודת המבט של הלקוח (הצגת ממשק עריכת הזמנות, ניהול מלאי וכו').

**בתוך כל תיקיית Feature יש סדר קבוע:**
- **`views/` או קבצי JSX ראשיים**: העמודים עצמם שאותם המשתמש רואה ופונה אליהם בניתוב (כמו `ProductManagement.jsx` או `CatalogView.jsx`).
- **`components/`**: רכיבי UI ששייכים **רק** לפיצ'ר הזה. (לדוגמה: `CartItemCard` ישב רק תחת ה-components של ה-cart).
- **`hooks/`**: חילוץ הלוגיקה העסקית וה-State מהעמוד הראשי לתוך פונקציות עצמאיות (לדוגמה: `useCartActions.js` תחת cart, או `useProductData.js` תחת catalog). מודל זה מפריד בין איך שהאפליקציה נראית (UI) לבין איך שהיא מתנהגת.
- **`__tests__/`**: בדיקות האוטומציה הרלוונטיות לפיצ'ר עצמו, מוודאות שהלוגיקה לא נשברת כשעושים שינויים.

---

## זרימת המידע במערכת (Data Flow Example)
כדי להבין איך הכל מתחבר, נבחן את התהליך של **"הוספת בד לעגלת הקניות"**:

1. **(Frontend - Component)** המשתמש לוחץ על `AddToCartButton.jsx` בעמוד המוצר.
2. **(Frontend - Hook)** הפעולה הזאת קוראת לפונקציה מתוך הקובץ `useCartForm.js` שמעבירה את המידע.
3. **(Frontend - API)** ה-Hook קורא ל-Axios (בתיקיית `api/client.js`) ושולח בקשת `POST /orders/cart`.
4. **(Backend - Route)** בקשת ה-HTTP מגיעה לשרת ונתפסת על ידי ה-Route המתאים ב-`domains/orders/routes.py`.
5. **(Backend - Schema)** השרת מוודא שהמידע תקין (Validation) באמצעות Pydantic Schema ב-`domains/orders/schemas.py` (מוודא שהאורך תקין ושנשלחו כל הנתונים).
6. **(Backend - Service)** אחרי אישור הנתונים, הפונקציה מועברת ל-`domains/orders/services.py` שם מופעלת **הלוגיקה העסקית**: ה-Service בודק במסד הנתונים האם נשאר מספיק מלאי, האם המוצר פתוח להזמנות עונתיות כרגע, ורושם את השורה בעגלה.
7. **(Backend - Model)** הרישום עצמו מתבצע מול הטבלה שהוגדרה ב-`domains/orders/models.py`.
8. המידע חוזר באותו המסלול עד שהוא מעדכן את ה-UI.

הארגון הזה מבטיח שאם נרצה, למשל, לשנות את האופן שבו מוסיפים משהו לעגלה (עיצוב), נגע רק בקומפוננטה בצד הלקוח. אם נרצה לשנות את החוק העסקי של "למי מותר להוסיף לעגלה" ניגע רק ב-Service בשרת.

---

## 3. מחזור חיי הזמנה וסטטוסים (Order Lifecycle)

המערכת מנהלת את ההזמנות במחזור חיים קבוע שמורכב מסטטוסים ברורים, על מנת להפריד בין פעולות שלקוח מבצע בעגלה לפעולות שמורידות מלאי או דורשות תשלום.

1. **סטטוס עגלה (`cart`)**:
   - לקוח מוסיף מוצר לעגלה.
   - המוצר נוסף לעגלה ומסומן ב-V (בממשק הלקוח).
   - מבחינת היסטוריית הזמנות, נוצרת עבור הלקוח "הזמנה" וירטואלית אחת שבה מופיעים הפריטים במצב 🛒 `cart`.
   - **מלאי:** הוספה לעגלה **אינה** מורידה מלאי מהמערכת.

2. **שליחת הזמנה (`order_unpaid`)**:
   - הלקוח לוחץ על כפתור "שלח הזמנה" בראש דף העגלה.
   - **מלאי:** הכמות שהוזמנה **יורדת באופן מיידי** מהמלאי הזמין (מוקצית ללקוח).
   - ההזמנה נוצרת במערכת (מעבר מ-CartItem ל-Order) ועוברת למצב 📦 `order` (לא שולם).

3. **תשלום מאובטח (`order_paid`)**:
   - הלקוח לוחץ על כפתור "לתשלום מאובטח" (בתחתית דף העגלה, או מתוך הזמנה קיימת שטרם שולמה).
   - **מלאי:** אם לא נשלחה הזמנה קודם לכן - המלאי יורד. אם כבר נשלחה הזמנה - המלאי כבר ירד.
   - ההזמנה עוברת למצב 💲 📦 `order` (שולם).
   - *הערה: נכון לגרסה הנוכחית, כפתור זה כבוי מכיוון שטרם חוברה מערכת סליקה.*

4. **נעילת עונה / חיתוך (`cutting`)**:
   - לאחר תאריך הדד-ליין של העונה, כל ההזמנות הפתוחות ננעלות ולא ניתן להוסיף או להסיר פריטים - רק לשלם (אם עוד לא שולם).
   - הסטטוס משתנה ל-✂️ `cutting` (בתוספת 💲 אם שולם).

5. **סגירת עונה / ארכיון (`archived`)**:
   - לאחר שהמשלוח סופק בפועל ללקוח, האדמין לוחץ על כפתור "סגור עונה" בטאב "הכנה לעונה".
   - כל ההזמנות של אותה עונה עוברות לסטטוס ארכיון (`archived`).
   - בעונה הבאה, מספור ההזמנות של הלקוח יקודם הלאה (+1).

---

## 4. DevOps וניהול תצורה (Infrastructure & Logging)

### ניהול לוגים באמצעות Docker
ניהול הלוגים במערכת (עבור השרת, מסד הנתונים ו-Redis) מנוהל באופן מרוכז ישירות על ידי מנוע ה-Docker עצמו, כפי שמוגדר בקובץ `docker-compose.prod.yml`. 
משמעות הדבר היא שאין צורך לנהל כתיבה לקבצי לוגים בתוך קוד הפייתון או להגדיר שכבות לוגים מורכבות במערכת ההפעלה.

**מנגנון הפעולה:**
- נעשה שימוש ב-Docker Logging Driver מסוג `json-file`.
- כל הפלט המודפס למסך (`stdout` / `stderr`) מהאפליקציה נשאב אוטומטית על ידי דוקר ונשמר.
- מופעל מנגנון **Log Rotation** מובנה למניעת סתימת הדיסק הקשיח: הוגדרה מגבלת גודל של 3MB לקובץ ומקסימום 4 קבצים היסטוריים לכל שירות (סה"כ ~12MB לכל היותר לשירות). ברגע שהמכסה מתמלאת, דוקר מוחק אוטומטית את הקבצים הישנים ביותר ושומר רק את העדכניים.

### חיבור יציב למסד הנתונים (Connection Pooling)
על מנת להבטיח זמינות מלאה ולמנוע שגיאות אקראיות של ניתוקים (כמו "MySQL Server has gone away"), מערכת ה-Backend משתמשת במנגנון Pooling מתקדם.
המערכת מוגדרת להחזיק עד 10 חיבורים קבועים (ו-20 חיבורים לחריגות עומס), מפעילה `pool_recycle` (רענון חיבורים מדי שעה) ובעיקר מפעילה `pool_pre_ping` שדוגם את החיבור שנייה לפני שליפת נתונים ומתחבר מחדש באופן שקוף במקרה של קריסת תקשורת.

### מטמון וביצועים (Caching Strategy)
על מנת לאפשר טעינה מהירה של החנות ולמנוע עומס על מסד הנתונים, המערכת משתמשת ב-**Redis** כדי לשמור בזיכרון מטמון (Cache) את קטלוג המוצרים המלא.
**מנגנון ריענון אוטומטי (Cache Invalidation):**
- קטלוג המוצרים נשמר במטמון עם מפתח קבוע (`full_product_catalog`) ופג תוקף של שעה.
- עם זאת, המערכת מתוכננת לאיפוס **אקטיבי ומיידי**: בכל פעם שמתבצעת פעולת עריכה ב-Admin (הוספת דגם, עדכון מלאי, מחיקת מוצר, או אפילו סידור מחדש של סדר התצוגה), מופעלת הפונקציה `_invalidate_catalog()` שמנקה את המטמון מיד.
- כך מובטח שהגולשים באתר תמיד יראו את המלאי העדכני ביותר ללא עיכובים, ועם זאת ייהנו מזמני טעינה מהירים בין עדכון לעדכון.

---

## 5. ניטור, תעבורה ואבטחה (Monitoring & Traffic)

המערכת משתמשת בשילוב של שלושה כלים מרכזיים כדי להבטיח יציבות, אבטחה, ומעקב צמוד על כל מה שקורה באפליקציה בכל השכבות:

### א. Nginx (ניהול תעבורה ופרוקסי)
Nginx מתפקד כשומר הסף המרכזי של המערכת (Reverse Proxy). כל בקשה שמגיעה מהאינטרנט פוגשת קודם כל אותו.
**אופן השימוש בפרויקט:**
- **ניתוב (Routing):** ניתוב בקשות ה-API ל-Backend, ניתוב משתמשים רגילים לאפליקציית ה-React (ה-Frontend), וניתוב מאובטח לסאב-דומיין `monitor.bingo.online` עבור מערכת הניטור.
- **הגנה מפני עומסים (Rate Limiting):** Nginx מגביל כמות בקשות לשנייה כדי למנוע התקפות DDoS או סריקות של בוטים. הוגדרה הגבלה גלובלית לכל האתר, והגבלה מחמירה יותר ספציפית על נתיב ההתחברות (Login).
- **פתרון בעיות DNS דינמי:** Nginx מוגדר להשתמש ב-Resolver הפנימי של דוקר (`127.0.0.11`) בשילוב משתנים. הדבר מבטיח שגם אם אחד הקונטיינרים (כמו ה-Backend או מערכת הניטור) קורס ועולה מחדש עם כתובת IP פנימית שונה, Nginx ידע לזהות את הכתובת החדשה אוטומטית ולא יחזיר שגיאות `502 Bad Gateway`.

### ב. Beszel (ניטור תשתית ושרת)
כלי ניטור קל-משקל (Lightweight) שנועד לנטר את ה"ברזלים" (התשתית) שעליהם יושבת המערכת. 
**אופן השימוש בפרויקט:**
- מותקן כצמד קונטיינרים בדוקר: ה-Hub (שומר את הנתונים ומציג את הדשבורד) וה-Agent (הסוכן שאוסף את המידע מהשרת).
- **ניטור משאבים:** עוקב בזמן אמת אחר צריכת ה-CPU, זיכרון ה-RAM, שטח האחסון בדיסק, ותעבורת הרשת של השרת הפיזי.
- **ניטור דוקר:** מציג בדיוק כמה זיכרון וכוח עיבוד לוקח כל קונטיינר בנפרד (למשל `fabric_backend` מול `fabric_redis`), ומאפשר לזהות דליפות זיכרון (Memory Leaks) או עומסים נקודתיים של שירות ספציפי.
- הגישה אליו מאובטחת וחווטה ישירות רק ל-Nginx המקומי, כך שהפורטים שלו אינם חשופים החוצה לאינטרנט.

### ג. Sentry.io (ניטור אפליקטיבי ושגיאות קוד)
בניגוד ל-Beszel שמנטר את החומרה, Sentry מנטר את ה**תוכנה** עצמה (Application Performance Monitoring & Error Tracking).
**אופן השימוש בפרויקט:**
- **לכידת שגיאות (Error Tracking):** כל שגיאה או חריגה (Exception) שקורית בקוד הפייתון (למשל שגיאת 500) נשלחת אוטומטית לממשק של Sentry יחד עם כל ה-Stack Trace והשורה המדויקת שבה הקוד נפל. זה חוסך את הצורך לחפש שגיאות בתוך קבצי לוגים ארוכים.
- **ניתוח ביצועים (Performance):** מנטר זמני תגובה של נתיבים שונים ב-API. הוא יכול להראות לנו אם שאילתה מסוימת למסד הנתונים לוקחת יותר מדי זמן ומעכבת את הלקוח.
- **מעקב צד לקוח:** מנטר גם תקלות שקשורות ישירות לדפדפן של הלקוח (למשל קריסות של קומפוננטות ב-React או מכשירי מובייל מסוימים) שאי אפשר לראות בלוגים של השרת.
