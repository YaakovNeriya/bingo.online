# נוהל שחזור והתאוששות מאסון (Disaster Recovery)

מסמך זה מתאר את הפעולות המדויקות שיש לבצע במקרה שהעלתה לפרודקשן קורסת (למשל, ה-Health Check בפייפליין נכשל והאתר למטה).

מכיוון שאנו עובדים בגישת GitOps ללא פריסה מדורגת אוטומטית (Blue/Green), שחזור המערכת דורש התערבות אנושית קצרה ומהירה.

## תרחיש חירום: הפריסה נכשלה והאתר למטה

אם פריסה ל-Production מסתיימת בכישלון, בצע את סדר הפעולות הבא כדי להחזיר את האתר לאוויר בתוך דקות:

### שלב 1: שחזור מסד הנתונים לאחור (Downgrade)
אם העדכון שגרם לקריסה כלל מיגרציית מסד נתונים (Alembic), עליך לבטל אותה כדי שהקוד הישן יוכל לחזור לעבוד.
התחבר לשרת ה-VPS (פרודקשן) והרץ את הפקודה הבאה מתוך תיקיית הפרויקט:

```bash
# הרצת פקודת חזרה לאחור (ביטול המיגרציה האחרונה)
sudo docker compose -f docker-compose.prod.yml run --rm backend alembic downgrade -1
```
*הערה: אם הפריסה לא כללה שינוי במסד הנתונים אלא רק שינוי קוד (למשל צבע כפתור ב-React), אתה יכול לדלג על שלב זה.*

### שלב 2: ביטול הקוד השבור ב-GitHub (Revert)
הדרך הבטוחה ביותר להחזיר את הקוד הישן היא דרך גיטהאב, כך שהפייפליין יבנה מחדש את התמונות ויעלה אותן בצורה מסודרת:
1. היכנס למסך ה-**Commits** בפרויקט שלך ב-GitHub.
2. מצא את ה-Commit האחרון שגרם לתקלה.
3. לחץ עליו, ובצד ימין למעלה לחץ על **Revert**. פעולה זו תיצור Commit חדש שמבטל את השינויים של הקוד השבור.
4. מכיוון שהגדרנו אישור ידני, גש ללשונית **Actions**, בחר ב-**Build Production**, ולחץ על **Run workflow** כדי לפרוס את הקוד המתוקן (הישן).

### תרחיש קיצון: השחתת נתונים (Hard Restore)
אם המיגרציה מחקה בטעות עמודות קריטיות עם נתוני לקוחות (Data Loss), ביצוע `downgrade` לא יחזיר את המידע! במצב כזה יש לשחזר את הגיבוי מ-Google Drive:
1. הורד את קובץ הגיבוי האחרון (קובץ `.sql.gz`) מתיקיית ה-`backup` ב-Google Drive.
2. העבר אותו לשרת ה-VPS.
3. חלץ וייבא אותו ישירות למסד הנתונים:
```bash
zcat backup_DD-MM-YY.sql.gz | sudo docker compose -f docker-compose.prod.yml exec -T db mysql -u root -p fabric_db
```
4. בצע Revert לקוד ב-GitHub כפי שמתואר בשלב 2.

---
> [!TIP]
> **תרגול:** מומלץ לבצע ניסוי "על יבש" של נוהל השחזור הזה בסביבת ה-Staging לפחות פעם ברבעון, כדי לוודא שאתה שולט בתהליך ומוכן לרגע האמת.
