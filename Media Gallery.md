# תוכנית עבודה מפורטת: ארכיטקטורת גלריית המדיה (Unified Media Gallery)

אנו הולכים לכתוב מחדש את לוגיקת הצגת המדיה בדף המוצר. המטרה היא מעבר חלק בין **כל** התמונות והסרטונים של המוצר (הדגם + כל הצבעים) בגלריה אחת רציפה, תוך סנכרון מלא דו-כיווני עם בועות הצבע, ותמיכה מלאה במסך מלא (Zoom & Video).

## שלב 0: גיבוי קבצי ליבה (Preparation)
לפני כל שינוי, יש ליצור קובץ גיבוי (`.bak`) עבור כל אחד מ-7 הקבצים הבאים המרכיבים את הגלריה:
1. `frontend/src/features/catalog/hooks/useProductData.js`
2. `frontend/src/features/catalog/views/ProductDetailView.jsx`
3. `frontend/src/features/catalog/components/product/ProductImageGallery.jsx`
4. `frontend/src/features/catalog/components/product/ColorSelector.jsx`
5. `frontend/src/components/ui/ImageCarousel.jsx`
6. `frontend/src/components/ui/ImageLightbox.jsx`
7. `frontend/src/components/ui/SmartImage.jsx`

---

## שלב 1: יצירת מערך המדיה האחיד (useProductData.js)
1. ניצור פונקציית עזר שתקבל את ה-`productModel` ותחזיר מערך שטוח `unifiedMedia` שמכיל אובייקטים: `{ url, type: 'video' | 'image', skuId: null | id }`.
2. סדר המערך יהיה **קשיח**: 
   - קודם כל סרטון הדגם (אם קיים).
   - לאחר מכן תמונת הדגם (אם קיימת).
   - לאחר מכן לולאה על כל ה-`color_skus` והכנסת כל התמונות שלהם לפי הסדר.
3. ניצור סטייט `currentMediaIndex` שמתחיל מ-0.
4. כניסה ראשונית לדף: ה-`selectedSku` יאותחל ל-`null` תמיד (כדי שאף בועה לא תהיה מסומנת). התמונה הראשונה המוצגת תהיה אינדקס 0 (דגם).
5. פונקציית סנכרון (`onIndexChange`): כשמגללים בגלריה לאינדקס מסוים, נבדוק את ה-`skuId` באותו אינדקס מתוך המערך המאוחד, ונעדכן את ה-`selectedSku` בהתאם (כדי לסמן את הבועה).

## שלב 2: העברת המערך ל-Views (ProductDetailView.jsx)
1. במקום להעביר ל-`ProductImageGallery` רק את `selectedSku`, נעביר לה את `unifiedMedia` השלם יחד עם `currentMediaIndex` ופונקציית הסטייט לשינוי האינדקס.

## שלב 3: סנכרון בועות הצבע (ColorSelector.jsx)
1. בעת לחיצה על בועת צבע, הלוגיקה **לא** תחליף את מערך התמונות ולא תאתחל אינדקס ל-0.
2. הלוגיקה תמצא את האינדקס הראשון בתוך `unifiedMedia` שבו ה-`skuId` שווה ל-ID של הצבע שנלחץ.
3. נקרא ל-`setCurrentImageIndex(foundIndex)` כדי להקפיץ את הגלריה הראשית והקרוסלה לאותו אינדקס בצורה חלקה.

## שלב 4: עדכון רכיב הגלריה (ProductImageGallery.jsx)
1. הקומפוננטה תעביר את ה-`unifiedMedia` במלואו אל ה-`ImageCarousel` ואל ה-`ImageLightbox`.
2. נוודא שהגלריה מגיבה לשינויי אינדקס מבחוץ ומעבירה אותם פנימה, וכן מקבלת עדכוני אינדקס מהקרוסלה כשמחליקים עם האצבע.
3. בסגירת ה-Lightbox, נעביר חזרה ל-Gallery את האינדקס האחרון שבו עצרנו במסך המלא, כדי שהגלריה תקפוץ בדיוק לשם בסגירה.

## שלב 5: התאמת הקרוסלה (ImageCarousel.jsx)
1. הקרוסלה תרנדר כעת אובייקטים של מדיה מתוך `unifiedMedia` (במקום רשימת מחרוזות URLs כפי שהיה עד כה).
2. הוספת תמיכה אמיתית ב-`activeIndex` חיצוני: 
   - שימוש ב-`useEffect` שמאזין ל-`activeIndex` שמגיע מבחוץ, וקורא ל-`emblaApi.scrollTo(activeIndex)` אם הוא השתנה. זה יאפשר ללחיצה על צבע לנווט באנימציה חלקה למיקום הנכון.

## שלב 6: התאמת המסך המלא וחסימת החלקה (ImageLightbox.jsx)
1. ה-Lightbox יקבל אף הוא את מערך ה-`unifiedMedia` וירונדר בהתאם.
2. הלוגיקה המטפלת ב-Zoom (משתנה `isZoomed`) תורחב:
   - כרגע היא חוסמת רק גרירה של העכבר/מגע (`watchDrag`).
   - יש לעדכן את מאזין המקלדת (חצי ימינה/שמאלה) כך שאם הסטייט `isZoomed` פועל, לחיצה על חיצים **לא** תעביר תמונה, אלא נשהה את המעבר עד ליציאה מהזום.
3. בפעולת `onClose`, נקרא ל-`onClose(currentIndex)` כדי לסנכרן אחורה לגלריה.

## שלב 7: וידאו בלחיצה בלבד (SmartImage.jsx)
1. נשאיר את טעינת Wistia החכמה מבוססת `IntersectionObserver` כי היא מעולה לביצועים (Fast Loading).
2. התנהגות `autoplay`: כרגע הסרטון מתנגן אוטומטית ברגע שה-Lightbox נפתח. 
   - נשנה זאת: הנגן של Wistia **לא** יקבל אוטומטית `autoplay=true`.
   - התוצאה: במסך מלא (או בגלריה), יופיע כפתור הפליי המובנה של Wistia.
   - הסרטון יופעל אך ורק אם המשתמש לוחץ עליו פיזית (כפי שביקשת: "הסרטון צריך לפעול אך ורק מתי שאנחנו לוחצים על הפליי שלו").
   - מעבר לסרטון על ידי החלקה פשוט יציג אותו מושהה (Paused) בהמתנה ללחיצת פליי.

---

## תהליך הבדיקה (Verification Plan)
1. **טעינה ראשונית:** וידוא שהתמונה/סרטון המוצגים הם של הדגם הכללי, ואף צבע לא מסומן כנבחר.
2. **החלקה חלקה:** גרירה שמאלה/ימינה עוברת בין הדגם לכל הצבעים ברצף, תוך כדי שבועות הצבע נדלקות ונכבות בהתאם לאינדקס הנוכחי.
3. **לחיצה על צבע:** הקלקה על בועה מבצעת החלקה מהירה (`scrollTo`) ישירות לתמונה הראשונה של אותו הצבע.
4. **מסך מלא (Lightbox):** 
   - פתיחת המסך המלא שומרת על האינדקס.
   - ביצוע Zoom-In חוסם מעבר החלקה (גם מגע וגם חיצי מקלדת).
   - יציאה חזרה לדף המסך הרגיל מסנכרנת את תמונת הרקע לתמונה שבה היינו במסך המלא.
5. **וידאו:** הוידאו טוען מהר כרגיל, אך לא מתחיל לנגן לבד - רק אחרי לחיצה על פליי.
