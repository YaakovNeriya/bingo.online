# תוכנית עבודה להכנת המערכת לייצור (Production Readiness)

המטרה: תשתיות, דוקר, שרת ה-Oracle, רשת, אבטחה היקפית, ואוטומציה (CI/CD).


### 1. כותרות אבטחה (Security Headers ב-Nginx)
כדי למנוע מתקפות כמו הזרקת קוד זדוני (XSS), ניסיונות לגרום לאתר שלנו להיטען בתוך "מסגרת" של אתר אחר (Clickjacking), וגניבת נתונים, אנחנו נוסיף כותרות אבטחה קריטיות להגדרות ה-Nginx שלנו:
* `X-Frame-Options: DENY` - חוסם אתרים אחרים מלהציג את האתר שלנו בתוך ה-Iframe שלהם.
* `X-Content-Type-Options: nosniff` - מונע מהדפדפן לנחש סוגי קבצים, מה שחוסם סוג מסוים של מתקפות XSS.
* `Content-Security-Policy` (CSP) - קובע בדיוק מאיזה מקורות מותר לאתר לטעון סקריפטים, תמונות וגופנים.

### 2. דחיסת נתונים בשרת (Nginx Gzip Compression)
**המצב כרגע:** שרת ה-Nginx שולח את קבצי האתר (JS, CSS) לדפדפן בצורה "גולמית". הקבצים האלו מכילים המון טקסט ואפשר לדחוס אותם בקלות.
**התיקון:** נוסיף ל-`nginx.conf` שורות שמפעילות דחיסת `gzip`. זה יכווץ את משקל האתר בכ-70% לפני שהוא נשלח ללקוח, ויגרום לאתר להיטען בשבריר שנייה.

### 3. הצפנת תעבורה (HTTPS / TLS עם Let's Encrypt)
הערה סופר-קריטית. בלי HTTPS, כל הסיסמאות, פרטי ההתחברות ונתוני הלקוחות עוברים ברשת כטקסט גלוי (Plain text), וכל שאר אמצעי האבטחה שלנו הופכים לחסרי משמעות.
לכן, נוסיף לשרת תעודת אבטחה חינמית של **Let's Encrypt**. נשנה את הגדרות ה-Nginx שלנו כך שיאזין לפורט 443 (HTTPS המאובטח), ויעשה הפניה (Redirect) אוטומטית לכל מי שמנסה להיכנס לאתר דרך HTTP רגיל (פורט 80).

### 4. שמירת נתונים קבועה (Docker Volumes)
כדי למנוע מצב שבו עדכון גרסה מוחק נתונים (כמו הזמנות או תמונות שהעלית), אנחנו משתמשים בשני **Volumes** (אזורי אחסון קבועים בדיסק של השרת):
1. **`db_data`**: שומר את כל מסד הנתונים. גם אם נמחוק את השרת ונקים אותו מחדש, הנתונים יישארו בטוחים.
2. **`backend_uploads`**: שומר את כל תמונות הבדים שאתה מעלה. הגדרנו שגם שרת ה-FastAPI (ששומר את התמונות) וגם שרת ה-Nginx (שמציג אותן ללקוחות) חולקים את אותה תיקייה בדיוק, ככה שהכל מסונכרן ומהיר.

### 5. אופטימיזציית שרתים (Docker Multi-stage & Slim Images)
כבר הטמענו טכנולוגיות מתקדמות כדי שהאתר ירוץ מהר ולא יצרוך זיכרון מיותר על השרת החינמי:
* **Multi-stage Build בצד לקוח:** קובץ ה-`Dockerfile.prod` של ה-Frontend משתמש בשיטה של "בנייה רב-שלבית". בשלב הראשון הוא מקמפל את קוד ה-React לקבצים סטטיים קטנטנים, ובשלב השני הוא מעביר רק את הקבצים המוכנים לשרת Nginx. זה מקטין את משקל המערכת במאות מגה-בייטים (הוא זורק לפח את כל ה-`node_modules` לאחר הקימפול).
* **Slim Images בצד השרת:** ה-Backend משתמש בגרסת `python:3.11-slim`, שהיא גרסה רזה ומכווצת של פייתון שמיועדת בדיוק לסביבות ייצור, מה שחוסך משאבי מעבד וזיכרון (RAM) על שרת ה-Oracle שלנו.

### 6. אבטחה והרשאות (Non-Root User)
כברירת מחדל, דוקר מריץ תוכנות עם הרשאות מנהל (Root) בתוך הקונטיינר. במקרה של פריצה לקוד, זה עלול לאפשר לתוקף להשתלט על השרת המארח. לכן נשנה את ה-`Dockerfile.prod` שלנו:
* נייצר משתמש "רגיל" (ללא הרשאות ניהול) בתוך שרת ה-Python ונגדיר שהאפליקציה תרוץ רק תחתיו.
* נוודא שהמשתמש הזה מקבל הרשאות קריאה וכתיבה אך ורק לתיקיית התמונות (`/app/uploads`), כדי שיוכל לשמור תמונות חדשות, אבל לא לשנות שום קובץ קריטי אחר במערכת.

### 7. ניהול תהליכים נכון (Gunicorn במקום Uvicorn רגיל)
כדי להבטיח יציבות מקסימלית, לא נשתמש ב-Uvicorn הבסיסי עם דגל `--workers` (שעלול להפיל את כל הקונטיינר אם Worker אחד קורס). במקום זאת, נתקין את **Gunicorn** כמנהל תהליכים (Process Manager) מעל Uvicorn. 
Gunicorn יודע לזהות Workers שנתקעו, לסגור אותם בעדינות, ולהעלות חדשים במקומם מבלי שהמשתמשים ירגישו שום ניתוק, מה שמבטיח "אפס נפילות" במקרה של שגיאת קוד נקודתית.

### 8. "דופק חיים" (Health Checks) ולוגים מרכזיים
כשהאתר רץ בייצור (Production), אנחנו חייבים דרך לדעת שהוא באמת "נושם" בלי שניאלץ להיכנס אליו פיזית כל חצי שעה.
* **Health Check (בדיקת דופק):** נייצר בקוד של השרת כתובת נסתרת (למשל `api/healthz/`). נגדיר לדוקר לפנות לכתובת הזו במרווחי זמן קבועים (לדוגמה, פעם בדקה או פעם ב-5 דקות). הבדיקה הזו לא צורכת שום משאבים. אם השרת מחזיר תשובה של "אני חי והתחברתי למסד הנתונים בהצלחה", הכל טוב. אם השרת נתקע ולא עונה - דוקר יבין שהשרת מת ויעשה לו הפעלה מחדש (Restart) אוטומטית לגמרי כדי לחסוך זמן נפילה (Downtime).
* **לוגים מרכזיים (Logging):** במקום שנצטרך להתחבר לשרת ולהקליד פקודות מסובכות כדי לקרוא שגיאות, נגדיר שהמערכת תאסוף את שגיאות הקריטיות מכל הקונטיינרים (React, Python, מסד נתונים) ותשמור אותם בקובץ אחד מסודר.

### 9. הגבלת משאבים והגנה מקריסות (Docker Limits)
**המצב כרגע:** הקונטיינרים בדוקר רצים ללא גבולות זיכרון. אם יש "זליגת זיכרון" (Memory Leak) בקוד של פייתון, הוא יכול "לשתות" את כל ה-24GB של שרת האורקל עד שהשרת הפיזי יקרוס לגמרי. בנוסף, מוגדר `restart: always` שיכול לגרום ללולאת הפעלות-מחדש אינסופית.
**התיקון:** נוסיף ב-`docker-compose.prod.yml` מגבלות זיכרון (למשל גג 1GB לשרת) ונשנה את ההגדרה ל-`restart: unless-stopped`, מה שיבטיח שגם במקרה קיצון - השרת לעולם לא ייחנק.

### 10. תסריט גיבוי אוטומטי לענן חיצוני (Offsite Backups)
* אצור לך קובץ סקריפט `scripts/backup_db.sh`.
* הסקריפט הזה ידע להתחבר למסד הנתונים (MySQL), לכווץ את כל הנתונים של האתר (משתמשים, הזמנות, מלאי) לקובץ גיבוי עם תאריך (למשל `backup_2026_06_23.sql.gz`).
* **העברה לענן חיצוני:** כפי שציינת בצדק, גיבוי על השרת עצמו לא שווה הרבה במקרה של קריסת שרת או כופרה. לכן, הסקריפט שלנו ישתמש בכלי בשם `rclone` (או ב-AWS CLI) כדי להעלות את הגיבוי באופן אוטומטי ל-Google Drive שלך (או ל-AWS S3 / Oracle Object Storage). כך הנתונים שלך בטוחים ב-100% גם אם השרת הפיזי נשרף.

### 11. ייצור סיסמאות קריפטוגרפיות (Secret Generation)
* אצור לך סקריפט קטן שתוכל להריץ פעם אחת, והוא ייצר עבורך מחרוזת אבטחה (SECRET_KEY) חזקה ובלתי ניתנת לפיצוח שאותה תשים בשרת באורקל.

### 12. בדיקת שחזור (Restore Testing)
יש משפט מפורסם בהנדסת תוכנה שאומר: "גיבוי שלא נוסה מעולם - הוא לא גיבוי, הוא תקווה".
אם משהו חלילה קורס ורק אז אנחנו מגלים שקובץ הגיבוי שהסקריפט שלנו יצר כל יום הוא בעצם פגום או ריק - איבדנו הכל. 
לכן, יחד עם סקריפט הגיבוי, נייצר סקריפט נוסף שנקרא `test_restore.sh`. התפקיד שלו יהיה לקחת קובץ גיבוי אקראי מהענן ולנסות "לפתוח" אותו על מסד נתונים זמני (Test DB) כדי לוודא שכל ההזמנות והמוצרים באמת נמצאים שם והגיבוי תקין ב-100%.

### 13. סריקת אבטחה אוטומטית ב-CI/CD (Dependency Scanning)
**המצב כרגע:** צינור ה-GitHub Actions שלנו לוקח את הקוד ומיד בונה ומעלה אותו לשרת. אם במקרה השתמשנו בספריית פייתון שהתגלתה בה פרצת אבטחה (CVE) היום בבוקר, לא נדע על זה ונעלה קוד פגיע לשרת.
**התיקון:** זה באמת תיקון של 3 שורות. נוסיף לקובץ ה-`deploy.yml` בדיקה עם `pip-audit` (עבור ה-Backend) ו-`npm audit` (עבור ה-Frontend) לפני שהוא מרשה לעצמו לארוז את האתר לדוקר. אם יש פרצה ידועה - התהליך ייעצר וישלח לך אזהרה לגיטהאב.

### 14. פריסה אוטומטית מלאה מהענן (Fully Automated SSH Deployment)
**בקשת הלקוח:** "אני רוצה שהוא ימשוך הכול מגיטהאב".
**המצב כרגע:** היינו צריכים להיכנס לשרת (Oracle) ידנית כדי לכתוב את קובץ ה-`.env` ולהפעיל את הקוד.
**התיקון:** ניקח את האוטומציה לקצה! אנחנו נערוך את קובץ ה-`deploy.yml` כך שבסיום הבנייה, גיטהאב יתחבר עצמאית לשרת ה-Oracle שלך (באמצעות מפתח SSH מאובטח). הוא ייצור שם את קובץ ה-`.env` בעצמו על ידי שאיבת *כל הסודות* מ-GitHub Secrets, יעתיק את ה-`docker-compose.prod.yml`, ויפעיל את האתר מחדש.
זה אומר שמבחינתך – כל ניהול השרת מתבצע מתוך גיטהאב. אתה לא תצטרך להתחבר לשרת לעולם בשביל לעדכן סיסמה או להעלות גרסה.

### 15. שדרוג מסד הנתונים אוטומטית (Automated Alembic Migrations)
**המצב כרגע:** אם נוסיף בעתיד "קופון הנחה", נצטרך להתחבר ידנית לשרת ולהוסיף עמודה למסד הנתונים.
**התיקון:** נוסיף ל-`deploy.yml` פקודת שדרוג אוטומטית. כשנעלה קוד חדש, גיטהאב ישדרג את מסד הנתונים בעצמו ללא מגע יד אדם.