# משימות פיתוח טרום-ייצור (Pre-Production)

המטרה: כל שינויי הקוד, הלוגיקה, והאופטימיזציה שצריכים להתרחש בתוך אפליקציית ה-FastAPI וה-React עצמן, לפני שבכלל נוגעים בשרת האמיתי.


### 1. אבטחת שגיאות קוד (Exception Handling)
* נוסיף קוד ל-`backend/app/core/exceptions.py` ש"תופס" שגיאות פנימיות וקריסות (שגיאה 500).
* במקום שהשרת יחזיר למשתמש את הקוד שקרס (מה שיכול לחשוף להאקרים איך האתר בנוי), השרת יחזיר הודעה פשוטה "Internal Server Error" אבל ירשום את השגיאה האמיתית בקובץ הלוג שלך.

### 2. חיבור חכם למסד הנתונים (Connection Pooling & Pre-Ping)
**המצב כרגע:** בקובץ `database.py`, פייתון פותח חיבור אסינכרוני למסד הנתונים, אבל אין לו הגנה מפני קריסות תקשורת (למשל אם MySQL סוגר חיבורים ישנים). זה עלול להוביל לשגיאות 500 אקראיות של "MySQL Server has gone away".
**התיקון:** נוסיף הגדרות של `pool_size`, `max_overflow`, והכי חשוב `pool_pre_ping=True` שבודק את החיבור שנייה לפני שהוא שולף נתונים, ופותח חיבור חדש אוטומטית אם הקודם מת.

### 3. סנכרון נתונים חי (Cache Invalidation)
**המצב כרגע:** בקובץ `admin/routes.py`, כשהמנהל מוסיף דגם חדש או משנה מלאי, המערכת לא מעדכנת את זיכרון המטמון (Redis) של הגולשים. המשמעות היא שגולשים יראו מלאי ישן או לא יראו מוצרים חדשים במשך שעה שלמה (עד שה-Cache פג תוקף).
**התיקון:** נוסיף פונקציה שמנקה את ה-Cache באופן אוטומטי (`delete_cache`) בכל פעם שהמנהל עושה פעולת יצירה, עדכון או מחיקה.

### 4. כיווץ תמונות חכם בהעלאה (Smart Image Compression)
**בקשת הלקוח:** "צריך סקריפט שמכווץ... ושלא יתקע את השרת. ומה אם התמונה מוואטסאפ וכבר קטנה?"
**המצב כרגע:** תמונות נשמרות "כפי שהן".
**התיקון:** נייצר סקריפט עיבוד תמונות אסינכרוני עם `Pillow`. הסקריפט יהיה **חכם**:
1. הוא יבדוק קודם את משקל התמונה (למשל: האם היא שוקלת פחות מ-300KB?).
2. אם התמונה הגיעה מוואטסאפ וכבר קטנה מאוד - הסקריפט יוותר על כיווץ אגרסיבי כדי לא להרוס את האיכות, ורק ימיר אותה בעדינות ל-WebP.
3. אם התמונה שוקלת 5MB - הוא יכווץ אותה ויקטין לה רזולוציה.
הכל יתבצע ברקע (`asyncio.to_thread`) מבלי לתקוע את השרת.

### 5. סריקת תמונות רטרואקטיבית (Retroactive Image Compression)
**המצב כרגע:** יש כבר תמונות שהועלו בעבר לשרת לפני שהפעלנו את מנגנון הכיווץ החכם.
**התיקון:** נייצר ונריץ סקריפט חד-פעמי שיעבור על כל התמונות הקיימות בתיקיית `backend_uploads/` ויכווץ גם אותן לפי אותה הלוגיקה, כדי לחסוך מקום מידי בשרת.

### 6. ניטור תקלות אקטיבי (Sentry Error Tracking)
**המצב כרגע:** אם קורית קריסה בשרת או שגיאה בדפדפן של הלקוח, המידע נשמר בקובץ טקסט שקשה לקרוא.
**התיקון:** נוסיף מערכת Sentry. כל שגיאה תישלח מיד למייל שלך עם שורת הקוד המדויקת שגרמה לה, כדי שנוכל לתקן לפני שהלקוח שם לב.

### 7. סגירה חלקה של שרת (Graceful Shutdown)
דיברנו על Gunicorn, אבל כשמכבים קונטיינר בדוקר (למשל בעדכון גרסה), האם השרת חותך באכזריות לקוחות שבאותו רגע בדיוק לחצו "שלם"? האם הוא משאיר חיבורים "פתוחים" למסד הנתונים (Zombie Connections)? 
**התיקון הדרוש:** הוספת "אירועי חיים" (Lifespan Events) ל-FastAPI שמוודאים שכאשר השרת מקבל פקודת כיבוי, הוא מסיים לטפל בבקשות הפעילות וסוגר את החיבור ל-MySQL בצורה מסודרת.

### 8. טעינת קוד חכמה ב-React (Lazy Loading)
**המצב כרגע:** בקובץ `App.jsx`, כל האתר (דף הבית, העגלה, הפאנל ניהול הכבד, ודף האודות) נטען בבת אחת ברגע שהלקוח נכנס. המשמעות היא שהלקוח מוריד מגה-בייטים של קוד שהוא אולי לא יצטרך לעולם (כמו קוד של פאנל הניהול).
**התיקון:** נעטוף את כל הראוטים ב-`React.lazy()` ו-`Suspense`. הלקוח יוריד רק את הקוד של הדף שהוא רואה באותו רגע. זה יקטין את זמן הטעינה הראשונית בעשרות אחוזים.

### 9. פיצול חבילות הקוד (Vite Chunk Splitting)
**המצב כרגע:** Vite אורז את כל הספריות החיצוניות (React, מפות, איקונים) יחד עם הקוד שלנו לקובץ JS אחד ענק. כשנעדכן משהו קטן בקוד שלנו, הלקוח ייאלץ להוריד מחדש את הכל.
**התיקון:** נוסיף ל-`vite.config.js` הגדרת `manualChunks` כדי לפצל את הספריות החיצוניות מהקוד שלנו. זה יאפשר לדפדפנים של הגולשים לשמור אותן בזיכרון מטמון (Cache) לטווח ארוך.

### 10. אופטימיזציית תמונות כבדות (WebP)
**המצב כרגע:** קבצי הרקע שיצרנו (למשל `Day_background.png` ו-`Night_background.png`) שמורים בפורמט PNG ששוקל המון מגה-בייטים. זה סותם את רוחב הפס של השרת וגורם לאתר להיראות "תקוע" למי שגולש מהסלולרי.
**התיקון:** נמיר את כל תמונות הרקע והטקסטורות הכבדות לפורמט `WebP` החדשני, ששוקל 80% פחות מ-PNG ושומר על אותה איכות בדיוק. הקוד של ה-CSS (ב-`index.css`) יתעדכן בהתאם.

### 11. קידום אתרים ושיווק (SEO & Open Graph)
אתר חנות בלי SEO הוא אתר בלתי נראה. כשלקוחה שולחת קישור של בד בוואטסאפ לחברה, האם מופיעה תמונה יפה של הבד עם כותרת ומחיר (Open Graph Tags)? האם לגוגל יש מפת אתר (sitemap.xml) וקובץ robots.txt שמנחה אותו איך לסרוק את האתר? 
**התיקון הדרוש:** בניית מערך Meta Tags דינמי ב-React לכל מוצר, ויצירת מפת אתר.

### 12. הקשחת CORS (מניעת גישה מאתרים זרים)
* נוסיף למערכת ההגדרות (`backend/app/core/config.py`) משתנה בשם `ALLOWED_ORIGINS`.
* נערוך את `backend/app/__init__.py` כך שבסביבת ייצור (Production), השרת יקבל בקשות **אך ורק** מהדומיין הרשמי של האתר (למשל `https://bingofabrics.co.il`), ולא יאפשר לכל אתר אחר לפנות אליו (במקום ה-`["*"]` שקיים כרגע).

### 13. חסימת מתקפות כוח גס ועומסים (Rate Limiting מורחב)
* נתקין ספרייה בשם `slowapi` במערכת (המבוססת על Redis או זיכרון מקומי).
* נוסיף הגבלת כמות בקשות (Rate Limits) למספר נקודות תורפה:
  - **התחברות (Login):** מקסימום 5 ניסיונות בדקה (נגד מתקפות ניחוש סיסמאות).
  - **הרשמה ויצירת הזמנות (Signup & Orders):** למנוע מבוטים לייצר מאות הזמנות פיקטיביות ומשתמשי ספאם (למשל 10 בקשות לדקה לכתובת IP).
  - **חיפוש (Search):** למנוע ממתחרים או בוטים "לשאוב" (Scrape) את כל הקטלוג ברגע או להעמיס על מסד הנתונים עם שאילתות כבדות.

### 14. בדיקות אוטומטיות (Unit & Integration Testing ב-CI)
ב-CI/CD שלנו (בגיטהאב) הגדרנו סריקת אבטחה, אבל מה עם סריקת תקינות? מה יקרה אם בטעות כתבנו באג שגורם לעגלת הקניות לחשב מחיר לא נכון? 
**התיקון הדרוש:** כתיבת סדרת בדיקות אוטומטיות (עם pytest ב-Backend). גיטהאב יריץ את הבדיקות האלו בכל פעם שתדחוף קוד, ואם החישובים לא יוצאים מדויקים - הוא יעצור את ההעלאה לשרת.


---

## סיום תכנון
התכנון הארכיטקטוני אושר. משתני סביבה ישמשו להגדרת דומיינים (תמיכה בסאב-דומיין חינמי), ומגבלת ההתחברות הוגדרה ל-5 פעמים בדקה.
