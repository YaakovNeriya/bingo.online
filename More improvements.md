!! מה קורה אם המשתמש פותח את האתר בשתי טאבים במקביל?

!! האם תמונות המוצרים עקביות באיכות וגודל?


עסקי / משפטי

תנאי שימוש ומדיניות החזרות — גורמים ישירות לאמון ולהחלטת קנייה
עמידה בתקנות מקומיות (ישראל: חוק הגנת הצרכן, חוק המסחר האלקטרוני)
מדיניות פרטיות תואמת GDPR / חוק הגנת הפרטיות הישראלי


## 1. עיצוב וחוויית משתמש (UX/UI)
מערכת ה-Glassmorphism והמעבר בין יום/לילה מציעים חוויית פרימיום, אך חייבים לוודא שאינם פוגעים בשימושיות (Usability).

* ⚠️ **בעיות קריטיות:** 
  * **נגישות (Accessibility):** יש לוודא ניגודיות טקסט (Contrast) מספקת, במיוחד במצב הלילה עם חומרים שקופים (Glassmorphism), כדי שהלקוחות יוכלו לקרוא את מחירי הבדים גם באור שמש.
שמקלדת ה-Numpad נפתחת אוטומטית בשדות הזנת טלפון/אשראי.
* 💡 **שיפורים מומלצים:** 
  * **חיווי ויזואלי להוספה לעגלה:** האנימציה של צמיחת הפרח על המטבע מצוינת, אך מומלץ להוסיף גם Toast Notification קטן (למשל: "בד משי אדום נוסף בהצלחה").

## 2. פסיכולוגיה ושיווק
אתר קניות צריך לטעת ביטחון מרבי בלקוח, במיוחד בחנות ייחודית כמו שלכם.
* ⚠️ **בעיות קריטיות:** 
  * **Trust Signals בקופה:** חייבים להציג בצורה בולטת אייקונים של כרטיסי אשראי מותרים, מנעול אבטחה, ומשפט מרגיע כמו "תשלום מאובטח ומוצפן" (Encrypted Checkout) ממש ליד כפתור התשלום.

  * **Urgency (דחיפות):** שילוב טקסט של "נשארו רק X מטרים במלאי!" כשכמות הבד יורדת מתחת לסף מסוים. זה מעלה דרסטית את אחוזי ההמרה.

  * **מדיניות משלוחים והחזרות:** הוספת אזור ברור בדף המוצר שמציין זמני משלוח ותנאי החזרת בדים.

  * **מניעת Cumulative Layout Shift (CLS):** יש להגדיר מידות ברורות (`width`, `height`) לתמונות המוצרים ב-CSS. זה מונע מהאתר "לקפוץ" כשהתמונות נטענות, מה שמונע הקלקות שגויות.
* 💡 **שיפורים מומלצים:** 
  * **Lazy Loading ו-Code Splitting:** פיצול הקוד ב-Vite וטעינת `Suspense` לקומפוננטות ראוטר הם הכרחיים לאתר React SPA כדי למנוע טעינת 100% מהקוד בכניסה לחנות.
* 🔮 **הזדמנויות עתידיות:** שימוש ב-CDN (כמו Cloudflare או AWS CloudFront) להגשת קבצי התמונות הכבדים של הבדים משרתי קצה קרובים ללקוחות.

## 4. אבטחה
הגנה על פרטיות הלקוחות ואמצעי התשלום היא הבסיס לכל מסחר אלקטרוני.

* ⚠️ **בעיות קריטיות:** 
  * **אבטחת תשלומים:** יש לוודא שהמערכת עומדת בתקן PCI-DSS. אסור בשום אופן לשמור מספרי אשראי גלויים במסד הנתונים, אלא לעבוד רק עם Token מספק סליקה חיצוני.
  * **CORS ו-HTTPS:** הפעלת תעודת Let's Encrypt והגבלת ה-CORS ב-FastAPI אך ורק לדומיין הייצור של החנות (כפי שציינתם במסמך).
  * **Rate Limiting:** יישום חסימות התחברות לנקודות התורפה (`/login`, `/checkout`) באמצעות `slowapi` כפי שתוכנן.
* 💡 **שיפורים מומלצים:** 
  * **הגנה על ממשק מנהל (Admin):** שינוי כתובת הפאנל למשהו קשה לניחוש (ולא `/admin`), והוספת Timeout אוטומטי (ניתוק) למנהל לאחר זמן אי-פעילות.
* 🔮 **הזדמנויות עתידיות:** הפעלת WAF (Web Application Firewall) כדי לחסום בוטים ותנועה זדונית לפני שהיא בכלל מגיעה לשרת ה-Nginx.

## 5. יציבות ואמינות
מה קורה כשיש עומס לקוחות בעונת החגים או סייל משמעותי?

* ⚠️ **בעיות קריטיות:** 
  * **ניהול מלאי קונקורנטי (Race Conditions):** יש לוודא שב-FastAPI, בזמן ההורדה מהמלאי במסד הנתונים (MySQL), יש שימוש בנעילות מתאימות (כגון `SELECT ... FOR UPDATE`) כדי למנוע מצב ששני לקוחות קונים במקביל את ה-2 מטר האחרונים מאותו גליל בד.
  * **תקלות סליקה (Graceful Degradation):** אם שרת הסליקה או חברת האשראי נופלים זמנית, הלקוח צריך לקבל הודעה מנומסת (למשל: "יש עיכוב קל בתקשורת מול חברת האשראי, אנא המתן...") ולא מסך שגיאה.
* 💡 **שיפורים מומלצים:** 
  * **חיבורים ל-DB (Pre-Ping):** הטמעת `pool_pre_ping=True` ב-SQLAlchemy היא חשובה מאוד כדי למנוע קריסות פתאומיות עקב התנתקות ה-MySQL.
* 🔮 **הזדמנויות עתידיות:** שימוש בתורים (Queues) כמו Celery לעיבוד הזמנות אסינכרוני במקרה של אלפי רכישות בדקה.

## 6. SEO וגילוי (Search Engine Optimization)
מערכות React שנוצרות עם Vite (Client-Side Rendering) מציגות אתגר גדול לגוגל.

* ⚠️ **בעיות קריטיות:** 
  * **פתרון SEO ל-SPA:** מכיוון שמדובר באפליקציית React נטו (ולא Next.js), הבוט של גוגל יראה קוד ריק בהתחלה. חובה ליישם פתרון כמו הגדרת Meta Tags בסיסיים שנטענים דרך השרת, או שימוש בשירות כמו Prerender.io.
  * **OG Tags דינמיים:** כאשר לקוחה משתפת בד בוואטסאפ, וואטסאפ אינו מריץ JavaScript! הוא חייב לקרוא את תגי ה-`og:image` ו-`og:title` מתוך ה-HTML הגולמי. שרת ה-FastAPI יצטרך אולי להגיש חלק מהדפים עם Meta מוזרק מראש (Server-Side Injection) כדי שהשיתוף בוואטסאפ יעבוד יפה כפי שתכננתם.
* 💡 **שיפורים מומלצים:** 
  * **Structured Data:** הטמעת סכמות של ארגון Schema.org (`Product`, `Offer`) כדי שגוגל יבין את מחירי הבדים ויציג אותם כסניפט עשיר (Rich Snippets) ישירות בתוצאות החיפוש.
  * יצירת `sitemap.xml` אוטומטי למוצרי החנות.

## 7. תפעול ומדרגיות (Operations)
ניהול שוטף ללא כאבי ראש.

* ⚠️ **בעיות קריטיות:** 
  * **גיבוי ובדיקת שחזור (Disaster Recovery):** תוכנית ה-Offsite Backups לענן החיצוני מצוינת. **חובה** לנסות את סקריפט `test_restore.sh` באופן ממשי לפחות פעם אחת לפני הדיפלוי, על מנת להבטיח שלא מגבים מסד נתונים ריק.
  * **הפרדת סביבות וסודות:** הבטחת העברת כל ה-`SECRET_KEY` וסיסמאות ה-DB דרך GitHub Secrets בסביבת הייצור ולא קודדוט (Hardcoded) בשום מקום.
* 💡 **שיפורים מומלצים:** 
  * **ניטור חי (Sentry):** חיבור Sentry כפי שמוזכר בתוכנית יאפשר זיהוי תקלות תשלום של משתמשים בזמן אמת ושליחת התראה לטלפון.
  * **לוגי אודיט (Audit Logs):** שמירת לוג מדויק על כל פעולה שמתבצעת בפאנל הניהול (מי שינה מלאי, מתי, ומה היה השינוי).
* 🔮 **הזדמנויות עתידיות:** שימוש ב-Docker Swarm בעתיד הקרוב כאשר מספר השרתים יגדל ויהיה צורך ב-Load Balancer כבד יותר.


