# מדריך פריסה ושיטת עבודה (GitOps Deployment Workflow)

מסמך זה מתאר את שיטת העבודה המדויקת שנקבעה לפרויקט (GitOps) וכיצד בנויים תהליכי הפריסה (CI/CD) לסביבות השונות. המטרה של תהליך זה היא להבטיח ששום קוד או שינוי במסד הנתונים לא יגיעו ללקוחות הקצה מבלי שנבדקו קודם לכן בצורה בטוחה.

## 1. מבנה הענפים (Branches) ב-Git

*   `staging` - **ענף הניסויים והבדיקות.** כל פוש לענף זה מעדכן באופן *אוטומטי* את שרת ה-Staging.
*   `main` - **ענף הפרודקשן (הקוד האמיתי).** פוש לענף זה *לא מעדכן שום דבר באופן אוטומטי*. פריסה מענף זה דורשת לחיצת כפתור ידנית בגיטהאב.

---

## 2. שגרת העבודה היומיומית (The Daily Workflow)

בכל פעם שאתה מפתח פיצ'ר חדש, מתקן באג או משנה את מסד הנתונים (Alembic), עליך לפעול בדיוק לפי סדר הפעולות הבא:

### שלב א': פיתוח ופוש לסטייג'ינג
1. אתה מפתח את הקוד שלך על ענף מקומי או ישירות על `staging`.
2. בסיום הפיתוח, בצע `git push` לענף `staging`.
3. **פעולה אוטומטית:** GitHub יתחיל מיד להריץ את ה-Pipeline שנקרא `Build Staging`. השרת ימשוך את הקוד, יריץ מיגרציות, ויעלה מחדש.

### שלב ב': בדיקת QA בסטייג'ינג
1. המתן שהפייפליין של הסטייג'ינג יסתיים ויסומן בירוק. (אם יש קריסה במיגרציה, הפייפליין יעצור ושרת הסטייג'ינג יושבת כדי למנוע קריסה מסוכנת).
2. היכנס לכתובת של אתר ה-Staging בדפדפן.
3. ודא שהקוד החדש עובד, שאין שגיאות קריטיות (Health Check), ושהפיצ'ר מתפקד כראוי.

### שלב ג': העברה לפרודקשן (אישור ידני)
רק לאחר שאתה בטוח ב-100% שהקוד תקין בסטייג'ינג:
1. בצע איחוד (Merge) של ענף ה-`staging` לתוך ענף ה-`main` בגיטהאב.
2. היכנס לגיטהאב לאזור ה-**Actions**.
3. בחר בתפריט הצדדי את ה-Workflow שנקרא **Build Production**.
4. לחץ על הכפתור הכחול **Run workflow**, ודא שהענף המסומן הוא `main`, ולחץ שוב לאישור.
5. **פעולה אוטומטית:** גיטהאב יפרוס את הקוד ללקוחות האמיתיים.

---

## 3. מנגנוני הגנה בפרודקשן (Safety Nets)

כאשר אתה לוחץ על הכפתור לפרוס לפרודקשן, הפייפליין מבצע סדרת בדיקות הגנה מאחורי הקלעים:

*   **גיבוי חכם (Conditional Backup):** לפני כל פריסה, הפייפליין בודק האם הקוד החדש שלך דורש עדכון של מסד הנתונים (SQL). אם כן, הוא מפעיל אוטומטית גיבוי לגוגל דרייב לפני תחילת המיגרציה.
*   **השבתה מבוקרת (Controlled Downtime):** ה-Backend מכובה לזמן קצר לפני המיגרציה כדי למנוע מצב שבו משתמשים עושים פעולות בזמן שמסד הנתונים משתנה (Race Condition).
*   **רולבק אוטומטי מלא (Automated Rollback):** 
    *   אם מיגרציית ה-SQL נכשלת (Alembic Error) – הפייפליין עוצר ומשחזר מיד את התמונות הישנות. התהליך מבוטל.
    *   אם הקוד החדש שובר את השרת ו-Deep Health Check מחזיר 500/503 – הפייפליין קורס, מבצע אוטומטית `downgrade -1` למסד הנתונים, מחזיר את תמונות ה-Docker הקודמות, ומרים חזרה את השרת התקין בתוך כ-10 שניות.

> [!NOTE]
> אם מנגנוני הרולבק האוטומטיים קורסים גם הם, עיין בקובץ `DISASTER_RECOVERY.md` לביצוע שחזור ידני של המערכת.
